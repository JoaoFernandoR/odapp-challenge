// Styles
import { HeaderContainer, Logo } from "./styles";

const Header = () => {
    return (
        <header>
            <HeaderContainer>
                <Logo> ODAPP </Logo>
            </HeaderContainer>
        </header>
    );
};

export default Header;
